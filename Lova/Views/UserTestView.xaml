<Page x:Class="Presentation.Views.UserTestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:Presentation.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserTest">

    <Page.DataContext>
        <vm:UserTestViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <ResourceDictionary Source="Resource\Style\Test.xaml"/>
    </Page.Resources>

    <Grid>
        <DataGrid ItemsSource="{Binding UserTests}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding TestName}" Header="Название теста" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding Result}" Header="Результаты" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding SolvedTime}" Header="Время прохождения" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
